<UserControl x:Class="Satisfactory_Universal_Tool.Views.BalancerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:Satisfactory_Universal_Tool.Localization">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <StackPanel Width="290" Margin="0,0,16,0">
                <TextBlock Text="{loc:Tr balancer.inputs}" FontWeight="SemiBold"/>
                <TextBox Text="{Binding InputsText}" AcceptsReturn="True" Height="90"
                         TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
            </StackPanel>
            <StackPanel Width="290">
                <TextBlock Text="{loc:Tr balancer.outputs}" FontWeight="SemiBold"/>
                <TextBox Text="{Binding OutputsText}" AcceptsReturn="True" Height="90"
                         TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12" VerticalAlignment="Center">
            <TextBlock Text="{loc:Tr balancer.tier}" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox ItemsSource="{Binding Tiers}" SelectedItem="{Binding SelectedTier}" Width="120"/>
        </StackPanel>

        <Button Grid.Row="2" Content="{loc:Tr common.calculate}" Command="{Binding CalculateCommand}"
                Width="120" HorizontalAlignment="Left" Padding="8,4" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="3" Text="{loc:Tr common.result}" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox Grid.Row="4" Text="{Binding ResultText, Mode=OneWay}" IsReadOnly="True"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" Background="#F5F5F5"/>
    </Grid>
</UserControl>